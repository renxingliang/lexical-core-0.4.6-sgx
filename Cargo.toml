# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies
#
# If you believe there's an error in this file please file an
# issue against the rust-lang/cargo repository. If you're
# editing this file be aware that the upstream Cargo.toml
# will likely look very different (and much more reasonable)

[package]
name = "lexical-core"
version = "0.4.6"
authors = ["Alex Huszagh <ahuszagh@gmail.com>"]
build = "build.rs"
autobenches = false
description = "Lexical, to- and from-string conversion routines."
documentation = "https://docs.rs/lexical-core"
readme = "README.md"
keywords = ["parsing", "lexical", "encoding", "no_std"]
categories = ["parsing", "encoding", "no-std"]
license = "MIT/Apache-2.0"
repository = "https://github.com/Alexhuszagh/rust-lexical/tree/master/lexical-core"
edition = "2018"

[profile.dev]
opt-level = 0
lto = false
debug = true
panic = "abort"

[profile.release]
opt-level = 3
lto = true
debug = false
debug-assertions = false
panic = "abort"

[lib]
crate-type = ["rlib"]
#crate-type = ["lib", "staticlib", "cdylib"]

[dependencies]
sgx_tstd    = { rev = "v1.0.9", git = "https://github.com/baidu/rust-sgx-sdk.git", optional = true }
cfg-if      = "0.1.9"
static_assertions = "0.3.3"
arrayvec    = { git = "https://github.com/renxingliang/arrayvec-sgx.git", optional = true, features = ["array-sizes-33-128"] }
dtoa        = { git = "https://github.com/renxingliang/dtoa-sgx.git", optional = true }
ryu         = { version = "1.0", optional = true }

[dev-dependencies.approx]
version = "0.3.0"

[dev-dependencies.proptest]
version = "0.9.4"

[dev-dependencies.quickcheck]
version = "0.8.5"
[build-dependencies.rustc_version]
version = "0.2"

[features]
default = ["mesalock_sgx", "correct", "ryu", "std"]
mesalock_sgx = ["std", "sgx_tstd"]
std = ["mesalock_sgx"]

correct = ["arrayvec", "table"]
grisu3 = ["dtoa"]
radix = []
rounding = []
table = []
trim_floats = []
unchecked_index = []
[badges.travis-ci]
repository = "Alexhuszagh/rust-lexical"
